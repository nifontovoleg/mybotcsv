"""
Пакет `handlers` содержит обработчики команд и сообщений.

Каждый файл обычно отвечает за одну логическую часть:
- start.py  — команда /start;
- tasks.py  — работа со списком задач (/add, /list, /list_csv).
"""

